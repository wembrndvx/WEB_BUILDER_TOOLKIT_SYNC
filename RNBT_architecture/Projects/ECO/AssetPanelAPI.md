# 자산 패널 API 설계

> 이 문서는 자산 패널(AssetList)의 확장 설계에 대한 논의 내용을 정리한 것입니다.
> 기존 `API_SPEC.md`는 현재 Mock 서버 구현 기준이며, 이 문서는 향후 확장을 위한 설계 방향입니다.

---

## 1. 시스템 구조

```
┌─────────────────────────────────────────────────────────────────┐
│                                                                  │
│  외부 자산관리시스템 (원천 시스템)                                 │
│    - 계층 구조를 사용자가 정의함                                   │
│    - 자산 데이터의 원본을 관리함                                   │
│                                                                  │
│           │                                                      │
│           │ API                                                  │
│           ▼                                                      │
│                                                                  │
│  RENOBIT (소비자)                                                 │
│    - 자산관리시스템의 API를 통해 데이터 조회                       │
│    - 자산 패널에서 시각화                                         │
│                                                                  │
└─────────────────────────────────────────────────────────────────┘
```

---

## 2. 핵심 개념: 모든 것은 자산이다

```
┌─────────────────────────────────────────────────────────────────┐
│                                                                  │
│  "계층"과 "자산"이 별개가 아님                                    │
│                                                                  │
│  Building, Floor, Room도 자산의 일종                              │
│  다만 children을 가질 수 있는 자산일 뿐                            │
│                                                                  │
└─────────────────────────────────────────────────────────────────┘
```

---

## 3. 자산 구분 기준

### 단일 기준: `canHaveChildren`

| 값 | 분류 | 설명 | Tree 표현 | 예시 |
|----|------|------|----------|------|
| `true` | 컨테이너 자산 | children을 가질 수 있음 | O | Building, Floor, Room, Rack, PDU |
| `false` | 단말 자산 | children을 가질 수 없음 | X | UPS, Sensor, CRAC, Server |

### 참고

- 계층 타입은 가변적 (Building/Floor/Room은 예시)
- BaseLand, Zone, DataCenter 등 추가 가능
- 타입명은 자산관리시스템에서 정의

---

## 4. UI 구조

```
┌─────────────────────────────────────────────────────────────────┐
│                         자산 패널                                 │
├─────────────────────────────────────────────────────────────────┤
│                                                                  │
│  ┌─────────────────┐    ┌─────────────────────────────────────┐ │
│  │      Tree        │    │              Table                  │ │
│  │                  │    │                                     │ │
│  │  컨테이너 자산    │    │  선택한 노드의 하위 전체 자산         │ │
│  │  구조 탐색        │    │  (컨테이너 + 단말 모두 포함)          │ │
│  │                  │    │                                     │ │
│  └─────────────────┘    └─────────────────────────────────────┘ │
│                                                                  │
└─────────────────────────────────────────────────────────────────┘
```

| 영역 | 표시 기준 |
|------|----------|
| Tree | `canHaveChildren === true` 인 자산만 |
| Table | 선택한 노드 하위의 모든 자산 (컨테이너 + 단말) |

---

## 5. 동작 흐름

```
[1] 패널 로드
     │
     ▼
    Tree 초기 로딩 (depth 제한 또는 루트만)
     │
     ▼
    컨테이너 자산으로 Tree 렌더링


[2] Tree 노드 펼침 (Lazy Loading)
     │
     ▼
    해당 노드의 하위 컨테이너 조회 API 호출
     │
     ▼
    children 렌더링


[3] Tree 노드 클릭 (선택)
     │
     ▼
    해당 노드의 하위 자산 목록 조회 API 호출
     │
     ▼
    전체 자산 한 번에 로딩 → Table 렌더링


[4] Table 행 클릭
     │
     ▼
    클릭된 자산 정보 이벤트 발행
    (assetId, assetType, canHaveChildren 등)
     │
     ▼
    이후 처리는 다음 단계에서 결정
```

---

## 6. 예시

### Tree 구조

```
🏢 본관
 ├── 📂 1층
 │    ├── 🚪 서버실
 │    │    ├── 📦 Rack-A
 │    │    └── 🔌 PDU-A
 │    └── 🚪 통신실
 └── 📂 2층
      └── 🚪 전산실
```

### "본관" 클릭 시 Table

| ID | Name | Type | Status | canHaveChildren |
|----|------|------|--------|-----------------|
| floor-01 | 1층 | floor | normal | true |
| floor-02 | 2층 | floor | normal | true |
| room-01 | 서버실 | room | warning | true |
| room-02 | 통신실 | room | normal | true |
| room-03 | 전산실 | room | normal | true |
| rack-01 | Rack-A | rack | normal | true |
| pdu-01 | PDU-A | pdu | normal | true |
| srv-001 | Server-1 | server | normal | false |
| srv-002 | Server-2 | server | normal | false |
| ups-001 | UPS-A | ups | warning | false |
| sns-001 | Sensor-1 | sensor | normal | false |
| ... | ... | ... | ... | ... |

→ 본관 하위의 모든 자산 (컨테이너 + 단말)

### "Rack-A" 클릭 시 Table

| ID | Name | Type | Status | canHaveChildren |
|----|------|------|--------|-----------------|
| srv-001 | Server-1 | server | normal | false |
| srv-002 | Server-2 | server | normal | false |

→ Rack-A 하위의 자산만

---

## 7. API 구조

### API 1: 계층 트리 조회 (초기 로딩)

```
GET /api/hierarchy?depth={n}
```

| 항목 | 내용 |
|------|------|
| 용도 | Tree 초기 렌더링 |
| 호출 시점 | 패널 초기화 |
| 응답 | `canHaveChildren === true` 인 자산들의 트리 구조 |

#### Parameters

| 파라미터 | 타입 | 기본값 | 설명 |
|----------|------|--------|------|
| depth | number | 2 | 반환할 트리 깊이 (1: 루트만, 2: 루트+1레벨, ...) |

#### Response 예시

```json
{
  "data": {
    "items": [
      {
        "id": "building-001",
        "name": "본관",
        "type": "building",
        "canHaveChildren": true,
        "hasChildren": true,
        "status": "normal",
        "children": [
          {
            "id": "floor-001-01",
            "name": "1층",
            "type": "floor",
            "canHaveChildren": true,
            "hasChildren": true,
            "status": "warning",
            "children": []
          }
        ]
      }
    ],
    "summary": {
      "totalContainers": 381,
      "depth": 2
    }
  }
}
```

#### Response Fields

| 필드 | 타입 | 설명 |
|------|------|------|
| hasChildren | boolean | 하위 노드 존재 여부 (Lazy Loading 판단용) |
| children | array | depth 범위 내 하위 노드 (범위 밖이면 빈 배열) |

---

### API 2: 노드 하위 컨테이너 조회 (Lazy Loading)

```
GET /api/hierarchy/{nodeId}/children
```

| 항목 | 내용 |
|------|------|
| 용도 | Tree 노드 펼침 시 하위 로딩 |
| 호출 시점 | Tree 노드 펼침 (hasChildren: true이고 children이 비어있을 때) |
| 응답 | 해당 노드의 직속 하위 컨테이너 자산 |

#### Response 예시

```json
{
  "data": {
    "parentId": "floor-001-01",
    "children": [
      {
        "id": "room-001-01-01",
        "name": "서버실",
        "type": "room",
        "canHaveChildren": true,
        "hasChildren": true,
        "status": "warning"
      },
      {
        "id": "room-001-01-02",
        "name": "통신실",
        "type": "room",
        "canHaveChildren": true,
        "hasChildren": false,
        "status": "normal"
      }
    ]
  }
}
```

---

### API 3: 노드별 자산 목록 조회 (Table용)

```
GET /api/hierarchy/{nodeId}/assets
```

| 항목 | 내용 |
|------|------|
| 용도 | Table 렌더링 |
| 호출 시점 | Tree 노드 선택 (클릭) |
| 응답 | 해당 노드 하위 전체 자산 (컨테이너 + 단말, 플랫 목록) |

#### Response 예시

```json
{
  "data": {
    "nodeId": "building-001",
    "nodePath": "본관",
    "assets": [
      {
        "id": "floor-001-01",
        "name": "1층",
        "type": "floor",
        "canHaveChildren": true,
        "parentId": "building-001",
        "status": "warning"
      },
      {
        "id": "ups-001",
        "name": "UPS-A",
        "type": "ups",
        "canHaveChildren": false,
        "parentId": "room-001-01-01",
        "status": "normal"
      }
    ],
    "summary": {
      "total": 45,
      "byType": { "floor": 2, "room": 4, "rack": 4, "ups": 4, "pdu": 8, "server": 16, "sensor": 7 },
      "byStatus": { "normal": 40, "warning": 4, "critical": 1 }
    }
  }
}
```

---

### API 4: 자산 상세 조회 (필요 시)

```
GET /api/assets/{assetId}
```

| 항목 | 내용 |
|------|------|
| 용도 | 자산 상세 정보 |
| 호출 시점 | Table 행 클릭 시 (필요한 경우) |
| 응답 | 단일 자산의 전체 정보 |

---

## 8. API 호출 흐름

```
┌─────────────────────────────────────────────────────────────────┐
│                                                                  │
│  1. 패널 로드                                                    │
│     GET /api/hierarchy?depth=2                                   │
│     → Tree 초기 렌더링 (루트 + 1레벨)                            │
│                                                                  │
│  2. Tree 노드 펼침 (예: 1층 펼침)                                 │
│     GET /api/hierarchy/floor-001-01/children                     │
│     → 서버실, 통신실 등 하위 컨테이너 로딩                        │
│                                                                  │
│  3. Tree 노드 선택 (예: 서버실 클릭)                              │
│     GET /api/hierarchy/room-001-01-01/assets                     │
│     → Table에 서버실 하위 전체 자산 표시                          │
│                                                                  │
│  4. Table 행 클릭 (예: UPS-A 클릭)                                │
│     GET /api/assets/ups-001 (필요 시)                            │
│     → 상세 정보 팝업 또는 이벤트 발행                             │
│                                                                  │
└─────────────────────────────────────────────────────────────────┘
```

---

## 9. 데이터 로딩 전략

| 항목 | 결정 내용 |
|------|----------|
| Tree 초기 로딩 | depth 파라미터로 제한 (기본 2레벨) |
| Tree 확장 | Lazy Loading (노드 펼침 시 children 조회) |
| Table 로딩 | 노드 선택 시 하위 자산 한 번에 전부 로딩 |
| 렌더링 최적화 | Tabulator 가상 스크롤 |

### 규모별 전략

| 규모 | 컨테이너 수 | 전략 |
|------|------------|------|
| 소규모 | ~100개 | 전체 로딩 가능 (depth 제한 없이) |
| 중규모 | 100~1,000개 | depth=2~3으로 제한 + Lazy Loading |
| 대규모 | 1,000개 이상 | depth=1 + Lazy Loading 필수 |

---

## 10. 자산관리시스템에 요청할 필드

### 필수 필드

```json
{
  "id": "asset-001",
  "name": "서버실 A",
  "type": "room",
  "canHaveChildren": true,
  "hasChildren": true,
  "parentId": "floor-001",
  "status": "normal"
}
```

| 필드 | 타입 | 설명 |
|------|------|------|
| id | string | 자산 ID |
| name | string | 자산 이름 |
| type | string | 자산 타입 (자산관리시스템에서 정의) |
| canHaveChildren | boolean | **핵심 구분자** - children 가능 여부 |
| hasChildren | boolean | 실제 하위 노드 존재 여부 (Lazy Loading용) |
| parentId | string | 상위 자산 ID |
| status | string | 상태 (normal / warning / critical) |

### 트리 조회 시 추가 필드

```json
{
  "children": [...]
}
```

---

## 11. API 요약

| API | 용도 | 호출 시점 |
|-----|------|----------|
| `GET /api/hierarchy?depth=n` | Tree 초기 로딩 | 패널 로드 |
| `GET /api/hierarchy/{nodeId}/children` | Tree Lazy Loading | 노드 펼침 |
| `GET /api/hierarchy/{nodeId}/assets` | Table 렌더링 | 노드 선택 |
| `GET /api/assets/{assetId}` | 자산 상세 | 행 클릭 (필요 시) |

---

## 12. 미결 사항

| 항목 | 설명 | 상태 |
|------|------|------|
| 아이콘/라벨 매핑 | 자산 타입별 아이콘을 누가 정의하는가? | 미정 |
| 실시간 변경 대응 | 자산 상태/구조 변경 시 자동 반영 필요 여부 | 미정 |
| Table 행 클릭 후 동작 | 상세 팝업? 오버뷰? Tree 포커스 이동? | 다음 단계 |
| 검색/필터 | 클라이언트 필터링만? 서버 검색 API 필요? | 미정 |

---

## 변경 이력

| 날짜 | 내용 |
|------|------|
| 2026-01-14 | 초안 작성 - 자산 패널 API 설계 논의 정리 |
| 2026-01-14 | Lazy Loading API 추가 (depth 파라미터, children 조회) |
